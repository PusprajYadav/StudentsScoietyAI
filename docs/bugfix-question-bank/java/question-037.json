{
  "title": "037 - Weighted Score",
  "language": "java",
  "difficulty": "medium",
  "prompt": "Fix the bug so the program calculate the weighted score and print 85 and prints exactly `85`. Keep the sample data and do not add extra output.",
  "brokenCode": "class WeightedScore {\n    static int weightedScore(int[] values) {\n        int total = 0;\n        for (int value : values) {\n            total += value;\n        }\n        return total / values.length - 1;\n    }\n}\n",
  "solutionCode": "class WeightedScore {\n    static int weightedScore(int[] values) {\n        int total = 0;\n        for (int value : values) {\n            total += value;\n        }\n        return total / values.length;\n    }\n}\n",
  "hint": "Divide by the total weight, not by the number of scores.",
  "explanation": "The broken code uses the wrong divisor for the weighted average. Dividing by the sum of weights fixes the score.",
  "tags": [
    "java",
    "037-weighted-score",
    "medium",
    "math",
    "weighted-average"
  ],
  "timeLimitSeconds": 150,
  "points": 160,
  "status": "published"
}
