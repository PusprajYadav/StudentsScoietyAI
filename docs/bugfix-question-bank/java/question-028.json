{
  "title": "028 - Contact Preference",
  "language": "java",
  "difficulty": "medium",
  "prompt": "Fix the bug so the program pick the first non-empty contact option and print email and prints exactly `email`. Keep the sample data and do not add extra output.",
  "brokenCode": "class ContactPreference {\n    static boolean contactPreference(String text, String query) {\n        return text.contains(query.toLowerCase());\n    }\n}\n",
  "solutionCode": "class ContactPreference {\n    static boolean contactPreference(String text, String query) {\n        return text.toLowerCase().contains(query.toLowerCase());\n    }\n}\n",
  "hint": "The final fallback should still be used if earlier options are empty.",
  "explanation": "The broken code stops before checking the final fallback. Evaluating all options in order fixes it.",
  "tags": [
    "java",
    "028-contact-preference",
    "medium",
    "fallback",
    "conditions"
  ],
  "timeLimitSeconds": 150,
  "points": 160,
  "status": "published"
}
