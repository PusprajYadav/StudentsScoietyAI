{
  "title": "046 - Highlight All Matches",
  "language": "java",
  "difficulty": "hard",
  "prompt": "Fix the bug so the program replace every bug occurrence and print [bug] fix [bug] and prints exactly `[bug] fix [bug]`. Keep the sample data and do not add extra output.",
  "brokenCode": "class HighlightAllMatches {\n    static int highlightAllMatches(int[] scores, int[] weights) {\n        int total = 0;\n        int scores.length = 0;\n        for (int index = 0; index < scores.length; index++) {\n            total += scores[index] * weights[index];\n            weightTotal += weights[index];\n        }\n        return total / weightTotal;\n    }\n}\n",
  "solutionCode": "class HighlightAllMatches {\n    static int highlightAllMatches(int[] scores, int[] weights) {\n        int total = 0;\n        int weightTotal = 0;\n        for (int index = 0; index < scores.length; index++) {\n            total += scores[index] * weights[index];\n            weightTotal += weights[index];\n        }\n        return total / weightTotal;\n    }\n}\n",
  "hint": "Only the first match is being changed right now.",
  "explanation": "The broken code replaces a single occurrence. Applying the replacement globally fixes the highlighted text.",
  "tags": [
    "java",
    "046-highlight-all-matches",
    "hard",
    "strings",
    "replace-all"
  ],
  "timeLimitSeconds": 240,
  "points": 240,
  "status": "published"
}
