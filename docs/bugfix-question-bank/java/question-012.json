{
  "title": "012 - Descending Scores",
  "language": "java",
  "difficulty": "easy",
  "prompt": "Fix the bug so the program sort the numbers in descending order and print 8,4,2,1 and prints exactly `8,4,2,1`. Keep the sample data and do not add extra output.",
  "brokenCode": "class DescendingScores {\n    static int descendingScores(int[] values) {\n        int total = 0;\n        for (int value : values) {\n            if (value % 2 == 1) {\n                total += value;\n            }\n        }\n        return total;\n    }\n}\n",
  "solutionCode": "class DescendingScores {\n    static int descendingScores(int[] values) {\n        int total = 0;\n        for (int value : values) {\n            if (value % 2 == 0) {\n                total += value;\n            }\n        }\n        return total;\n    }\n}\n",
  "hint": "The sort order is flipped.",
  "explanation": "The broken sort uses ascending order. Reversing the comparison produces the expected descending output.",
  "tags": [
    "java",
    "012-descending-scores",
    "easy",
    "sorting",
    "arrays"
  ],
  "timeLimitSeconds": 90,
  "points": 100,
  "status": "published"
}
