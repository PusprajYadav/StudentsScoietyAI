{
  "title": "024 - Countdown Sequence",
  "language": "csharp",
  "difficulty": "medium",
  "prompt": "Fix the bug so the program print the countdown as 3-2-1 and prints exactly `3-2-1`. Keep the sample data and do not add extra output.",
  "brokenCode": "public static class CountdownSequence\n{\n    public static string CountdownSequence(int year, int month, int day)\n    {\n        return year.ToString() + \"-\" + month.ToString() + \"-\" + day.ToString(\"00\");\n    }\n}\n",
  "solutionCode": "public static class CountdownSequence\n{\n    public static string CountdownSequence(int year, int month, int day)\n    {\n        return year.ToString() + \"-\" + month.ToString(\"00\") + \"-\" + day.ToString(\"00\");\n    }\n}\n",
  "hint": "The loop stops too early or includes one extra step.",
  "explanation": "The loop boundaries are wrong. Iterating from 3 down to 1 fixes the countdown.",
  "tags": [
    "csharp",
    "024-countdown-sequence",
    "medium",
    "loops",
    "ranges"
  ],
  "timeLimitSeconds": 150,
  "points": 160,
  "status": "published"
}
