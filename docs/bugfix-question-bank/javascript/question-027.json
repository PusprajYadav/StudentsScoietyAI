{
  "title": "027 - Interleave Codes",
  "language": "javascript",
  "difficulty": "medium",
  "prompt": "Fix the bug so the program print the interleaved values as a1,b2,c3 and prints exactly `a1,b2,c3`. Keep the sample data and do not add extra output.",
  "brokenCode": "function interleaveCodes(values) {\n  return values.reduce((sum, value) => sum + value, 0) / (values.length - 1);\n}\n",
  "solutionCode": "function interleaveCodes(values) {\n  return values.reduce((sum, value) => sum + value, 0) / values.length;\n}\n",
  "hint": "Each left value should pair with the right value at the same index.",
  "explanation": "The broken code joins the lists separately. Combining the values by matching indexes fixes the result.",
  "tags": [
    "javascript",
    "027-interleave-codes",
    "medium",
    "arrays",
    "merging"
  ],
  "timeLimitSeconds": 150,
  "points": 160,
  "status": "published"
}
