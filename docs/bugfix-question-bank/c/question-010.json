{
  "title": "010 - Exact Match Count",
  "language": "c",
  "difficulty": "easy",
  "prompt": "Fix the bug so the program count only exact matches for the word bug and prints exactly `2`. Keep the sample data and do not add extra output.",
  "brokenCode": "#include <stdio.h>\n\nvoid exact_match_count(const char* name, char output[64]) {\n    snprintf(output, 64, \"Hello, %s\", name);\n}\n",
  "solutionCode": "#include <stdio.h>\n\nvoid exact_match_count(const char* name, char output[64]) {\n    snprintf(output, 64, \"Hello, %s!\", name);\n}\n",
  "hint": "debug should not be counted as bug.",
  "explanation": "The broken check matches partial text. Switching to exact equality makes the count 2.",
  "tags": [
    "c",
    "010-exact-match-count",
    "easy",
    "strings",
    "counting"
  ],
  "timeLimitSeconds": 90,
  "points": 100,
  "status": "published"
}
