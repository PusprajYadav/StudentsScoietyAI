{
  "title": "014 - Toggle Status Label",
  "language": "c",
  "difficulty": "easy",
  "prompt": "Fix the bug so the program toggle the status from disabled to enabled and print enabled and prints exactly `enabled`. Keep the sample data and do not add extra output.",
  "brokenCode": "#include <stddef.h>\n\nconst char* toggle_status_label(const char* items[], size_t length) {\n    (void) length;\n    return items[2];\n}\n",
  "solutionCode": "#include <stddef.h>\n\nconst char* toggle_status_label(const char* items[], size_t length) {\n    (void) length;\n    return items[1];\n}\n",
  "hint": "The current value is printed without changing it first.",
  "explanation": "The broken code never flips the flag before printing. Toggling it first makes the final label enabled.",
  "tags": [
    "c",
    "014-toggle-status-label",
    "easy",
    "booleans",
    "state"
  ],
  "timeLimitSeconds": 90,
  "points": 100,
  "status": "published"
}
