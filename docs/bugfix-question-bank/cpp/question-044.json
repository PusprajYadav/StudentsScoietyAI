{
  "title": "044 - Most Frequent Word",
  "language": "cpp",
  "difficulty": "hard",
  "prompt": "Fix the bug so the program find the most frequent word and print bug and prints exactly `bug`. Keep the sample data and do not add extra output.",
  "brokenCode": "#include <string>\n\nstd::string mostFrequentWord(std::string text) {\n    size_t index = 0;\n    if ((index = text.find(\"bug\", index)) != std::string::npos) {\n        text.replace(index, 3, \"[bug]\");\n        index += 5;\n    }\n    return text;\n}\n",
  "solutionCode": "#include <string>\n\nstd::string mostFrequentWord(std::string text) {\n    size_t index = 0;\n    while ((index = text.find(\"bug\", index)) != std::string::npos) {\n        text.replace(index, 3, \"[bug]\");\n        index += 5;\n    }\n    return text;\n}\n",
  "hint": "Do not reset the best count while you scan.",
  "explanation": "The broken loop loses track of the highest frequency. Keeping the current best word and count fixes the answer.",
  "tags": [
    "cpp",
    "044-most-frequent-word",
    "hard",
    "arrays",
    "frequency"
  ],
  "timeLimitSeconds": 240,
  "points": 240,
  "status": "published"
}
